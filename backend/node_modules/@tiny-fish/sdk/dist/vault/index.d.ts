/**
 * Vault resource for managing credential vault connections and items.
 */
import { APIResource } from "../_utils/resource.js";
import { type VaultConnectResponse, type VaultDisconnectResponse, type VaultListConnectionsResponse, type VaultListItemsResponse, type VaultSyncItemsResponse } from "./types.js";
/** Manage vault provider connections. */
declare class VaultConnectionsResource extends APIResource {
    /** Connect a 1Password vault. */
    connect1Password(params: {
        token: string;
    }): Promise<VaultConnectResponse>;
    /** Connect a Bitwarden vault. */
    connectBitwarden(params: {
        clientId: string;
        clientSecret: string;
        masterPassword: string;
        serverUrl?: string;
    }): Promise<VaultConnectResponse>;
    /** List all vault connections. */
    list(): Promise<VaultListConnectionsResponse>;
    /** Disconnect a vault connection by ID. */
    disconnect(connectionId: string): Promise<VaultDisconnectResponse>;
}
/** Manage vault items. */
declare class VaultItemsResource extends APIResource {
    /** List all vault items across all connections. */
    list(): Promise<VaultListItemsResponse>;
    /** Sync items from all connected vault providers. */
    sync(): Promise<VaultSyncItemsResponse>;
}
/** Vault API namespace. */
export declare class VaultResource extends APIResource {
    readonly connections: VaultConnectionsResource;
    readonly items: VaultItemsResource;
    constructor(client: APIResource["_client"]);
}
export {};
//# sourceMappingURL=index.d.ts.map
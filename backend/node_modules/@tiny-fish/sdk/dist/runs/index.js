/**
 * Runs resource for retrieving and listing automation runs.
 */
import { SDKError } from "../_utils/errors.js";
import { APIResource } from "../_utils/resource.js";
import { runListParamsSchema, runListResponseSchema, runSchema, } from "./types.js";
/** Retrieve and list automation runs. */
export class RunsResource extends APIResource {
    /**
     * Retrieve a single run by its ID.
     *
     * Returns a promise that resolves with the full run details
     * including status, result, error, and browser configuration.
     *
     * @param runId - The run ID returned by `agent.queue()` or any run response.
     */
    async get(runId) {
        if (typeof runId !== "string" || !runId.trim()) {
            throw new SDKError("run_id must be a non-empty string");
        }
        const response = await this._client.get(`/v1/runs/${encodeURIComponent(runId)}`);
        return runSchema.parse(response);
    }
    /**
     * List automation runs, with optional filtering and pagination.
     *
     * Returns a promise that resolves with a paginated list of runs
     * and pagination info (total count, next cursor).
     */
    async list(params = {}) {
        const parsed = runListParamsSchema.safeParse(params);
        if (!parsed.success) {
            const issue = parsed.error.issues[0];
            throw new SDKError(issue?.message ?? "Invalid run list request");
        }
        const validParams = parsed.data;
        const queryParams = {};
        if (validParams.cursor !== undefined)
            queryParams["cursor"] = validParams.cursor;
        if (validParams.limit !== undefined)
            queryParams["limit"] = validParams.limit;
        if (validParams.status !== undefined)
            queryParams["status"] = validParams.status;
        if (validParams.goal !== undefined)
            queryParams["goal"] = validParams.goal;
        if (validParams.created_after !== undefined)
            queryParams["created_after"] = validParams.created_after;
        if (validParams.created_before !== undefined)
            queryParams["created_before"] = validParams.created_before;
        if (validParams.sort_direction !== undefined)
            queryParams["sort_direction"] = validParams.sort_direction;
        const hasParams = Object.keys(queryParams).length > 0;
        const response = await this._client.get("/v1/runs", hasParams ? { params: queryParams } : undefined);
        return runListResponseSchema.parse(response);
    }
}
//# sourceMappingURL=index.js.map
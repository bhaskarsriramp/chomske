/**
 * Wallet resource for retrieving wallet balance, auto-reload, and rates.
 */
import { APIResource } from "../_utils/resource.js";
import { walletResponseSchema } from "./types.js";
/** Retrieve the caller's wallet. */
export class WalletResource extends APIResource {
    /**
     * Get the caller's wallet: balance, auto-reload state, per-contract rates,
     * and any in-flight top-up.
     */
    async get() {
        const response = await this._client.get("/v1/wallet");
        return walletResponseSchema.parse(response);
    }
}
//# sourceMappingURL=index.js.map
node_modules/
build/
# Editor footage in local-storage mode (MEDIA_BUCKET unset). Never commit it.
backend/.media/
.env
.env.local
.env.*.local
backend/.env
npm-debug.log*
.DS_Store

# Test recordings (somebody's screen, tens of MB) and what was measured on them. Never commit.
backend/scripts/pointerTest/fixtures/
# Mouse logs from test recordings (qa/start-mouselog.cmd). Local only.
mouselogs/
